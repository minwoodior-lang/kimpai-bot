📌 홈 화면 – PRO 예측 구역 최종 레이아웃 (텍스트만으로 이해 가능하게 작성)

아래는 위에서부터 아래까지, 개발자가 그대로 코드로 옮기면 되는 정확한 구조 설명이야.

0️⃣ 전체 페이지 흐름 (텍스트 구조)
[오늘의 AI 김프 요약]          ← 기존 그대로 유지
(평균 김프 · 최대 김프 · 최소 김프 · 환율 등 텍스트)

<여기 아래에 기존 텍스트 전부 삭제>  ← 중요!

[PRO 전용 예측 카드]          ← 새로 만드는 컴포넌트 ①

[PRO 예측 차트 박스]           ← 새로 만드는 컴포넌트 ②

(그 아래는 기존 KR/해외 시세표 섹션 그대로 유지)

1️⃣ PRO 전용 예측 카드(컴포넌트 ①) – 텍스트 기반 구조

이 카드가 하나만 존재해야 함.
중요: 기존 빨간 박스 영역의 모든 텍스트/드롭다운은 완전히 삭제해야 함.

⭐ 카드 전체 구조 (그대로 구현 가능하도록 텍스트 설명)
──────────────────────────────────────────────────────────
[카드: 폭 max 900px, 좌우 라운딩, 배경 어둡게, thin border]

🔒 PRO 전용 48시간 김프 예측        ← 첫 줄, 좌측 정렬, 굵게

최근 30일 기준, 이 예측은 김프 2% 이상 급변 구간의 
90% 이상을 사전에 포착했습니다. (PRO 구독 시 전체 내용 확인 가능)
← 두 줄 자동 줄바꿈, 작은 회색 텍스트

[BTC Binance ▼]     ← 기존 MarketSelect 드롭다운 하나만 배치
──────────────────────────────────────────────────────────

텍스트만으로 카드가 어떻게 생겼는지 이해되는 버전
┌──────────────────────────────────────────────┐
│ 🔒 PRO 전용 48시간 김프 예측                 │
│                                              │
│ 최근 30일 기준, 이 예측은 김프 2% 이상 급변  │
│ 구간의 90% 이상을 사전에 포착했습니다.       │
│ (PRO 구독 시 전체 내용 확인 가능)            │
│                                              │
│ [ BTC Binance ▼ ]                            │
└──────────────────────────────────────────────┘


딱 이렇게 생긴 하나짜리 카드만 존재해야 해.

2️⃣ PRO 예측 차트 박스(컴포넌트 ②)

카드 바로 아래에 이어서 나오며 이것도 하나만 존재해야 함.

──────────────────────────────────────────────────────────
[차트 박스: 폭 max 900px, 라운드, 얇은 border, 높이 최소 260px]

(여기에 그래프 또는 예측 시각화가 렌더링됨)
──────────────────────────────────────────────────────────


텍스트로 보면 이렇게:

┌──────────────────────────────────────────────┐
│                 (  차트 영역  )              │
│                                              │
│   실제 48시간 예측 그래프가 들어가는 곳      │
└──────────────────────────────────────────────┘

3️⃣ 중요한 삭제 규칙 (텍스트로만 설명)

아래 요소들은 전부 제거해야 한다:

❌ 기존 요약 아래 위치하던 설명 텍스트
❌ 기존 드롭다운 박스
❌ 기존 예측 박스
❌ PRO 텍스트가 상단/우측 등에 중복 노출되는 모든 UI


즉, 최종 형태에서 예측 관련 UI는 딱 두 개만 존재해야 한다:

PRO 전용 예측 카드 ①

차트 박스 ②

4️⃣ 최종 완성 시 화면 흐름 (텍스트 화면도)
오늘의 AI 김프 요약
───────────────────────────
평균 김프: …
최대 김프: …
최소 김프: …
환율: …

┌───────────────────────────┐
│ 🔒 PRO 전용 48시간 김프 예측 │
│ 최근 30일 기준… (설명 텍스트) │
│ [BTC Binance ▼]             │
└───────────────────────────┘

┌───────────────────────────┐
│         (차트 영역)         │
└───────────────────────────┘

(아래 시세표 시작)

🔥 요약: 텍스트만으로 전달하는 최종 지시문

이 아래 한 문단만 개발자에게 보내도 바로 이해됨.

[개발자 전달용 단일 텍스트 지시문]

홈 화면에서 기존 예측 관련 요소(설명 텍스트, 드롭다운, 박스, PRO 텍스트 등)
전부 제거하고 아래 두 컴포넌트만 유지해주세요.

① "PRO 전용 48시간 김프 예측" 카드 (max-width 900px)
- 첫 줄: "🔒 PRO 전용 48시간 김프 예측"
- 두 줄 설명: 최근 30일 기준, 이 예측은 김프 2% 이상 급변 구간의 90% 이상을 사전에 포착했습니다. (PRO 구독 시 전체 내용 확인 가능)
- 아래에 MarketSelect 드롭다운 하나 배치

② 그 아래에 차트 박스 하나만 배치 (max-width 900px, min-height 260px)

이 두 개 외 기존 예측 관련 UI는 모두 삭제해야 하며,
특히 카드 외부에 남아 있는 설명 텍스트나 드롭다운이 하나라도 있으면 잘못 구현된 것입니다.