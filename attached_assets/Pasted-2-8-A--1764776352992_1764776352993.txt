2️⃣ 지금 진짜로 아직 안 된 것들(핵심만 요약)

이미 여러 번 말했는데 정리가 안 돼 있으니, 짧게만 다시 모아서 줄게요.
이 8가지만 제대로 해달라고 하면 됩니다.

A. 레이아웃/배경 관련

Layout.tsx 문법 에러 수정

169~170줄 부근 </div>, </nav> 닫는 태그 개수/순서 다시 맞춰서
JSX 문법 에러 전부 없애주세요.

배경 & 헤더 폭

전체 배경: 지금 진한 블랙 말고, 예전 네이비 그라디언트 톤으로 유지.

차트/코인 그리드를 둘러싸는 추가 검은 박스는 제거해서
컨텐츠가 바로 바탕색 위에 올라가게.

TopMarketInfoBar + 헤더(nav) 도 max-w-[1200px] mx-auto 안으로
메인 콘텐츠 폭과 완전히 딱 맞게 정렬.

라이트 모드 진짜 동작

ThemeProvider + darkMode: "class"는 이미 넣었다고 하니,

토글 눌렀을 때:

바탕: dark → 네이비, light → 거의 흰색

텍스트/버튼/테이블 색상도 **dark:/light:**로 다 보이게 실제로 적용되었는지 확인.

지금처럼 “아이콘만 바뀌고 화면 색은 그대로”인 상태면 안 됨.

B. 상단 “오늘의 AI 김프 요약” 블록

요약 섹션 레이아웃 & 문구

PC 기준:

왼쪽:
오늘의 AI 김프 요약 + 평균/최대/최소/환율 텍스트 (컴팩트하게)

오른쪽:

윗줄: KR Premium Score 카드 + PRO 분석 전체 보기 버튼

아랫줄: 🔒 PRO 전용 48시간 김프 예측 카드 +
아래 문구 그대로 전부 노출

텍스트 고정:

버튼: PRO 분석 전체 보기

설명문:

최근 30일 기준, 이 예측은 김프 2% 이상 급변 구간의 90% 이상을 사전에 포착했습니다. (PRO 구독 시 전체 내용 확인 가능)

모바일:

같은 내용 그대로 위에서 아래로 세로 스택

마지막 설명 문구까지 줄바꿈만 되고, 글자가 잘리지 않게 작은 폰트로라도 전부 보이게.

C. 코인 테이블 & 모바일 그리드

코인 셀 정렬 (아이콘 / 별 / 이름 두 줄)

한 셀 안에서 구조를 이렇게 맞춰 주세요:

<div className="flex items-center gap-3">
  <div className="flex flex-col items-center justify-center gap-1">
    <CoinIcon className="h-7 w-7" />
    <button className="text-[11px]">☆</button>
  </div>
  <div className="flex flex-col justify-center">
    <span className="text-sm font-medium truncate">{name_ko}</span>
    <span className="text-[11px] text-slate-400 uppercase">{symbol}</span>
  </div>
</div>


이렇게 해서 아이콘+별 세로중앙과 한글명/영문 심볼 두 줄의 높이가
KIMPGA처럼 딱 맞게 정렬되게.

테이블 행 스타일

index % 2 === 0 ? "bg-slate-800/30" 이런 줄무늬 로직 완전 제거.

공통 스타일:

다크: border-b border-slate-800 hover:bg-slate-900/60

라이트: border-b border-slate-200 hover:bg-slate-50

행 배경색은 단일, 행 사이 가는 라인으로만 구분.

모바일 5컬럼 한 화면에 들어오게

모바일(360~390px)에서 가로 스크롤 없이 아래 5개만 보이게:

코인명 / 현재가 / 김프 / 전일대비 / 거래액(일)

text-[10px], px-1.5, whitespace-nowrap 등으로 줄여서 맞추고,
필요하면 숫자만 남기는 식으로 정리.

D. 나머지 라이트/다크 & 검색/푸터

검색 인풋 / 푸터 라이트·다크 색

검색창, 푸터도

라이트: bg-white / text-slate-900 / border-slate-200

다크: bg-slate-900/80 / text-slate-100 / border-slate-700

라이트 모드에서 글자가 배경과 안 섞이고 확실히 읽히게.

3️⃣ 리플릿 쪽에 보낼 한 줄 정리 버전

위 내용 다 붙이기 길면, 짧게는 이렇게 보내도 됩니다:

Layout.tsx 169~170줄에 JSX 닫는 태그가 꼬여 있어서 syntax error 난 상태부터 먼저 고쳐 주세요.
그 다음에는

헤더 폭을 메인 max-w-[1200px]에 정확히 맞추고

예전 네이비 배경 + 라이트모드 실제 동작

상단 ‘오늘의 AI 김프 요약’ 섹션 레이아웃(왼쪽 텍스트 / 오른쪽 스코어+버튼+락 카드, 버튼 문구는 ‘PRO 분석 전체 보기’)

코인 셀 정렬(아이콘/별/코인명 두 줄) + 줄무늬 제거

모바일에서 5개 컬럼(코인명/현재가/김프/전일/거래액) 한 화면에 보이게

검색창/푸터도 라이트·다크 색상 분리
를 질문 없이 한 번에 적용해 주세요.