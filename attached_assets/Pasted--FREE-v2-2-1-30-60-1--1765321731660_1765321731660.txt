📌 [전달문] FREE 고래 시그널 v2.2 튜닝 요청
1. 동일 심볼 30분 → 60분 쿨다운으로 상향

목표
1시간 동안 같은 심볼이 여러 번 울리지 않도록,
심볼 단위 쿨다운을 30분 → 60분으로 변경해주세요.

수정 대상 파일

src/bot/schedulers/freeSignals.js
(FREE 고래 시그널 스케줄러 파일, canSendWhaleSignal / recordWhaleSignal 있는 곳)

구체 요청

현재 심볼별 쿨다운 상수(예: 아래와 비슷한 코드):

const SYMBOL_COOLDOWN_MINUTES = 30;


이 값을 60으로 변경해주세요.

const SYMBOL_COOLDOWN_MINUTES = 60;


canSendWhaleSignal() 에서 이 상수를 사용해
lastSentAt 기준으로 now - lastSentAt >= SYMBOL_COOLDOWN_MINUTES 를 체크하고 있다면,
추가 로직 변경 없이 상수값만 60으로 올리면 됩니다.

결과: 동일 심볼(예: TAO, DASH, BNB 등)은
최소 60분 간격으로만 텔레그램 고래 시그널이 발송되도록.

2. 보조지표 이모지 정리 + RSI 색상 추가

문제점

현재 보조지표 블록에서 각 줄이 이렇게 나옵니다:

- 200EMA: 상승 추세🟢 🔴
- RSI: 57.3
- MACD: 골든크로스🟢 🔴
- 캔들: 양봉🟢 🔴


라인마다 이모지가 두 개(🟢 🔴) 씩 붙어 지저분해 보이고

RSI는 색상/상태 정보가 없어서 직관성이 떨어집니다.

목표

각 보조지표 라인당 이모지는 1개만 사용

RSI도 과열/바닥/중립에 따라 색상 이모지 추가

매수/매도 방향(🟢/🔴) 이모지는 타이틀 줄에만 유지

🐋 WLD 고래 매수 활동 감지 [BINANCE] 🟢

2-1. 수정 대상 파일

src/workers/binanceSignalEngine.js
(고래 시그널 텍스트를 실제로 조합하는 부분 – buildWhaleSignalMessage 류 함수)

2-2. 보조지표 포맷 헬퍼 함수 추가

아래와 같은 형태로 공통 포맷 함수들을 추가해 주세요
(함수명/인자는 기존 코드에 맞게 조금 바꿔도 됩니다. 의미만 유지).

function formatEmaLine(trend) {
  // trend: "up" | "down" | "sideways"
  if (trend === "up") return "- 200EMA: 상승 추세 🟢";
  if (trend === "down") return "- 200EMA: 하락 추세 🔴";
  return "- 200EMA: 횡보 ⚪";
}

function formatRsiLine(rsi) {
  // rsi: number
  if (rsi >= 70) {
    return `- RSI: ${rsi.toFixed(1)} (과열 구간 🔴)`;
  }
  if (rsi <= 30) {
    return `- RSI: ${rsi.toFixed(1)} (저점/과매도 구간 🟢)`;
  }
  return `- RSI: ${rsi.toFixed(1)} (중립 ⚪)`;
}

function formatMacdLine(signal) {
  // signal: "bullish" | "bearish" | "neutral"
  if (signal === "bullish") return "- MACD: 골든크로스 🟢";
  if (signal === "bearish") return "- MACD: 데드크로스 🔴";
  return "- MACD: 중립 ⚪";
}

function formatCandleLine(isBull) {
  // isBull: boolean
  return isBull ? "- 캔들: 양봉 🟢" : "- 캔들: 음봉 🔴";
}

2-3. 보조지표 블록 조립 로직 교체

현재 보조지표 부분이 대략 이런 형태라면:

lines.push("📉 보조지표 (1시간 차트)");
lines.push(`- 200EMA: ${emaText}${emaEmoji} ${directionEmoji}`);
lines.push(`- RSI: ${rsi.toFixed(1)}`);
lines.push(`- MACD: ${macdText}${macdEmoji} ${directionEmoji}`);
lines.push(`- 캔들: ${candleText}${candleEmoji} ${directionEmoji}`);


⬇ 아래와 같이 변경해주세요:

lines.push("📉 보조지표 (1시간 차트)");
lines.push(formatEmaLine(emaTrend));        // up/down/sideways
lines.push(formatRsiLine(rsi));             // 과열/과매도/중립
lines.push(formatMacdLine(macdSignal));     // bullish/bearish/neutral
lines.push(formatCandleLine(isBullCandle)); // true/false


그리고 각 지표 라인 끝에 붙어 있던 directionEmoji(🟢/🔴) 는
전부 제거해 주세요.

매수/매도 방향은 맨 위 타이틀 라인에서만 유지하면 됩니다:

🐋 BNB 고래 매도 활동 감지 [BINANCE] 🔴

✅ 기대 효과

동일 심볼 도배 방지:
→ 동일 심볼 60분 쿨다운으로 텔레그램 알림 밀도 적당히 유지

메시지 가독성 향상:

보조지표 줄당 이모지 1개로 깔끔해짐

RSI 과열/바닥 구간을 색으로 직관적으로 표시

매수/매도 방향은 타이틀 한 곳에서만 강조